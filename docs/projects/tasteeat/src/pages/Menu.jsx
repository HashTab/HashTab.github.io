import MenuComponent from "../components/Menu/MenuComponent";

const Menu = () => {
  return (
    <main>
      <section className="Menu">
        <MenuComponent />
      </section>
    </main>
  );
};

export default Menu;
